"""The Observation Engine: Models, Collector, Builder, Validator — kept
together in one package because they are conceptually inseparable and
change together far more often than independently (ADR-006).
"""
